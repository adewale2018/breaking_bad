import React from "react";
import logo from "../../images/logo.png";

const Header = () => {
  return (
    <header className='center'>
      <img src={logo} alt='breaking bad brand logo' />
    </header>
  );
};

export default Header;
